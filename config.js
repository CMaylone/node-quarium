var config = {
  port: 3000, // Application port.
  morganLogFormat : 'dev', // The string pattern defining how morgan should log data.
  aquariumTempProbSerial: '000005507b51' // Serial Number of the device to read.
};

module.exports = config;